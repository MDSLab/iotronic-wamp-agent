============
Installation
============

At the command line::

    $ pip install iotronic-wamp-agent

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv iotronic-wamp-agent
    $ pip install iotronic-wamp-agent
